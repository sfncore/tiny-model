# Derived data (regenerate with: python curate.py)
dataset/

# Model weights and checkpoints (large files tracked via Git LFS)
# checkpoints/ — allow trained model checkpoints
*.bin
*.pt
*.pth

# Allow .gguf and .safetensors (tracked by LFS via .gitattributes)

# Python
__pycache__/
*.pyc
.venv/
venv/

# Training artifacts
wandb/
runs/
logs/

# Build tools (clone via: git clone --depth 1 https://github.com/ggml-org/llama.cpp.git tools/llama.cpp)
tools/

# Gas Town (added by gt)
.runtime/
.claude/
.logs/
state.json
